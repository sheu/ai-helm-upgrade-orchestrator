# Prometheus values — vehicle-services / eu-west-1 / PROD
component: prometheus
chart:
  repository: prometheus-community
  name: prometheus
  version: 25.0.0
image:
  repository: prom/prometheus
  tag: "v2.50.1"
  pullPolicy: IfNotPresent
replicaCount: 2
service:
  type: ClusterIP
  port: 9090
server:
  persistentVolume:
    size: 32Gi
  retention: 15d
  retentionSize: ""
