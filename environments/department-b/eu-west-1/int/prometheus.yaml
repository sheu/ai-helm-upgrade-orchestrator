# Prometheus values — vehicle-services / eu-west-1 / INT
component: prometheus
chart:
  repository: prometheus-community
  name: prometheus
  version: 25.0.0
image:
  repository: prom/prometheus
  tag: "v2.50.1"
  pullPolicy: IfNotPresent
replicaCount: 1
service:
  type: ClusterIP
  port: 9090
server:
  persistentVolume:
    size: 16Gi
  retention: 15d
  retentionSize: ""
